dist: focal
language: go
go:
- '1.16'
python:
- '3.6'
addons:
  apt:
    packages:
    - flawfinder
    - squashfs-tools
    - uuid-dev
    - libuuid1
    - libffi-dev
    - libssl-dev
    - libarchive-dev
    - libgpgme11-dev
    - libseccomp-dev
  homebrew:
    packages:
    - squashfs
    update: true
sudo: required
install:
- 
before_install:
- sudo chmod u+x .ci/*.sh
- "/bin/bash .ci/setup.sh"
before_script:
- export PATH=/bioinf-tools:$PATH
script:
- "/bin/bash .ci/build.sh"
notifications:
  email: false
